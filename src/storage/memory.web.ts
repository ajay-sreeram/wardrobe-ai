// Expo FileSystem is native-only. SQLite remains the web preview's local store;
// Markdown memory is initialized only in the iOS and Android app sandboxes.
export function initializeMemoryFiles() {}
